<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp">

    <TextView
        android:id="@+id/tvWelcome"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Dashboard Finance"
        android:textSize="26sp"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="32dp"/>

    <Button
        android:id="@+id/btnMenuValidasi"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="💳  Validasi Pembayaran"
        android:gravity="start|center_vertical"
        android:paddingStart="20dp"
        app:layout_constraintTop_toBottomOf="@+id/tvWelcome"
        android:layout_marginTop="40dp" />

    <Button
        android:id="@+id/btnMenuApproval"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="📝  Approval Purchase Request"
        android:gravity="start|center_vertical"
        android:paddingStart="20dp"
        app:layout_constraintTop_toBottomOf="@+id/btnMenuValidasi"
        android:layout_marginTop="16dp" />
</androidx.constraintlayout.widget.ConstraintLayout>